import {TaskSessionFinishRequest} from './task-session-finish-request.model';

describe('TaskSessionFinishRequest', () => {
  it('should create an instance', () => {
    expect(new TaskSessionFinishRequest()).toBeTruthy();
  });
});
